#!/usr/bin/env node
// `the-i18n-mcp` was renamed to `@the-i18n-kit/mcp`. MCP host configs that
// still say `npx the-i18n-mcp@latest` resolve here; importing the real entry
// starts the server, so the old name keeps tracking the current release.
import '@the-i18n-kit/mcp'
